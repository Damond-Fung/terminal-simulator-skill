# Example Input

Build an interactive terminal simulator for our docs site.

Requirements:

- Reuse the existing `terminal-learning-kit` project if possible
- Support a guided install walkthrough
- Show command and output separately
- Make it work on desktop and mobile web
- Provide command examples for PowerShell and Bash
- Keep reduced motion accessibility support
